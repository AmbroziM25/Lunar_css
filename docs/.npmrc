# Install the file:.. lunara-css dependency as a packed copy instead of a
# symlink — a symlink points back to the repo root, which contains this docs
# app itself, and that recursion breaks Next.js output file tracing on build.
# After changing framework CSS, re-run: npm install (then restart dev server).
install-links=true
